class Venda:
    def __init__(self, cliente_id, produto_id, quantidade, preco_total):
        self.cliente_id = cliente_id
        self.produto_id = produto_id
        self.quantidade = quantidade
        self.preco_total = preco_total
